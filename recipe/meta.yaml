{% set name = "andes" %}
{% set version = "0.6.9" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9b0490a5197469cb366a95696c58e485e8b4fbf3c01399f58613c1b0718daf0f

build:
  number: 0
  entry_points:
    - andes = andes.main:main
    - andesplot = andes.plot:main
    - tdsplot = andes.plot:tdsplot_main
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "
  skip: True  # [py<35]

requirements:
  host:
    - python
    - pip
  run:
    - python
    - cvxopt
    - numpy
    - scipy
    - matplotlib
    - pandas
    - sympy

test:
  imports:
    - andes
    - andes.config
    - andes.filters
    - andes.formats
    - andes.models
    - andes.routines
    - andes.utils
    - andes.variables
  requires:
    - pytest
  commands:
    - andes --help
    - andesplot --help

about:
  home: https://github.com/cuihantao/andes
  license: Apache Software
  license_family: APACHE
  license_file: LICENSE.txt
  summary: Another Power System Simulator
  doc_url: https://andes.readthedocs.io 
  dev_url: https://github.com/cuihantao/andes

extra:
  recipe-maintainers:
    - cuihantao
